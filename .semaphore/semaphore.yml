version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: install Deps
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
            - apt install git -y
            - apt install python3-pip -y
            - pip install python-telegram-bot --upgrade
